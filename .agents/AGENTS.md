# Automatic GitHub Deployment
Every time you make any changes to this repository, immediately run `git add .`, `git commit -m "..."`, and `git push` so the user can view the changes on their Vercel hosted site. Do this automatically after completing your work without waiting for explicit permission.
